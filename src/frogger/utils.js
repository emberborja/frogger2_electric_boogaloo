

/* // Check if proposed move is valid (on screen)
function isMoveOnScreen(x, y) {
    if (
        x >= 0 &&
        x < game.width - frogWidth &&
        y > rowHeight &&
        y < game.height - rowHeight
    ) {
        return true;
    } else {
        console.log("false", x, y);
        return false;
    }
} */